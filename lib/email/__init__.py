# lib/email - metalpy equivalent of Python's email package. Only
# lib/email/message.py (Message, MIME/header handling, parsing,
# serialization) exists so far - see PLAN_HTTP_CLIENT.md's note that
# lib/http/client.py's HTTPHeaders was built as a stand-in for this until a
# real email.message landed.
